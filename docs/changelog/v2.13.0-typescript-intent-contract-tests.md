# DELTA v2.13.0 — TypeScript Proof of Intent Contract Tests Changelog Entry

Type: TypeScript Proof of Intent readiness / contract test milestone

## Added

- `tools/delta_ts_intent_contract_tests.py`
- `docs/typescript-intent-contract-tests-v2.13.0.md`

## Purpose

This release adds Python-driven contract tests for the TypeScript Proof of Intent verifier after v2.12.0-v2.12.3.

It freezes machine-readable behavior for the selected Proof of Intent path.

## Scope

Tested:

- TypeScript build,
- canonical JSON vectors,
- schema verification,
- signed intent demo generation,
- positive Proof of Intent JSON verification,
- expired deadline rejection,
- JSON top-level contract,
- expected exit codes,
- expected Proof of Intent status fields.

## Security

This release adds no new cryptographic functionality.

It validates behavior only.

It does not prove legal identity, signer authority, global registry trust, policy correctness, regulatory compliance, or real-world truth.

## Tested

Expected checks:

```powershell
python tools\delta_ts_intent_contract_tests.py

python src/delta_cli.py verify-all
python tools\delta_canonical_json.py verify-vectors --vectors tests\vectors\canonical-json\vectors.json
git diff --check
```

## Next recommended milestone

```text
v2.14.0 — Private Evidence Commitment Profile
```
