# DELTA v2.16.10 — README 10-Minute Reviewer Guide Link / Heavy Test Readiness

## Executive summary

DELTA v2.16.10 adds the missing README-level link to the `Review DELTA in 10 Minutes` public reviewer guide.

After v2.16.9 introduced the dedicated 10-minute reviewer onboarding document, the public heavy test identified that the guide existed but was not explicitly linked from the README using the expected reviewer-facing marker.

This release closes that discoverability gap and improves public-review readiness.

## What changed

Updated:

- `README.md`

Added:

- `docs/changelog/v2.16.10-readme-10-minute-review-link.md`

## Strategic purpose

DELTA's public adoption path should be obvious from the repository front page.

The README now directly links to:

- the minimal local demo,
- the automated CI tamper-detection workflow,
- the quick-start path,
- `Review DELTA in 10 Minutes`,
- the reviewer checklist,
- the external review request.

This makes the public review path more complete and makes automated public-readiness checks stricter and more meaningful.

## Security impact

Documentation only.

No cryptographic verification semantics are changed.

No proof profile is modified.

No verifier behavior is changed.

No implementation behavior is changed.

No schemas, signatures, canonicalization rules, bundle rules, hash-binding rules, or proof verification logic are modified.

This release improves public reviewer discoverability while preserving DELTA's anti-overclaiming discipline.

## Tests performed

- `python src/delta_cli.py verify-all`
- `python tools\delta_canonical_json.py verify-vectors --vectors tests\vectors\canonical-json\vectors.json`
- `git diff --check`
- public heavy test marker check for `Review DELTA in 10 Minutes`

## Result

- `DELTA CLI RESULT: OK`
- `DELTA_JCS_VERIFY_OK=True`
- `git diff --check` completed without blocking errors
- README public-review marker present
