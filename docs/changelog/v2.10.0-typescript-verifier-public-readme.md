# DELTA v2.10.0 — TypeScript Verifier Public README Refresh Changelog Entry

Type: Documentation-only TypeScript verifier public-readiness milestone

## Added / Changed

- refreshed `verifier/ts/README.md`
- added `docs/typescript-verifier-public-readme-v2.10.0.md`

## Purpose

This release makes the TypeScript verifier README suitable for public review.

It documents:

- current TypeScript verifier capabilities,
- current TypeScript verifier limitations,
- install/build/test commands,
- schema verification commands,
- signed record verification commands,
- bundle verification commands,
- signed bundle verification commands,
- public wording,
- claims to avoid,
- security boundaries.

## Scope

Documentation only.

No verifier behavior changes.

No new cryptographic functionality.

## Security

This release reduces overclaiming risk.

It clearly states that the TypeScript verifier is experimental and that the Python implementation remains the Alpha Reference Implementation.

## Tested

Expected checks:

```powershell
cd verifier\ts
npm install
npm run build
npm run verify-vectors
npm run verify-schemas

cd ..\..
python src/delta_cli.py verify-all
python tools\delta_canonical_json.py verify-vectors --vectors tests\vectors\canonical-json\vectors.json
git diff --check
```

## Next recommended milestone

```text
v2.10.1 — TypeScript CLI Error Codes / Machine-Readable JSON Output
```
